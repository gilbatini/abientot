# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SITE_URL=

# Google Places API — powers the live reviews on /reviews.
# Enable "Places API (New)" in a Google Cloud project with billing on,
# then create an API key (restrict it to the Places API).
GOOGLE_PLACES_API_KEY=
